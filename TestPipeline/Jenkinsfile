pipeline {
    agent any
	tools
	{
		maven 'LocalMaven'
	}
    stages{
        stage('Build'){
            steps 
			{
                sh 'mvn clean package'
            }
            post {
                success 
				{
                    echo 'Start saving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}